#pragma once

#include <string>

namespace retrorts {

std::string LaunchGame(const std::string& console, const std::string& romPath, const std::string& cacheDir, const std::string& saveDir);

}  // namespace retrorts
